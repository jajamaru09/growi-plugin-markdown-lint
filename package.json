{
  "name": "growi-plugin-markdown-lint",
  "version": "0.1.0",
  "description": "GROWI plugin that formats Markdown via Prettier from a button on the editor toolbar",
  "type": "module",
  "keywords": ["growi", "growi-plugin", "markdown", "prettier", "format"],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "growiPlugin": {
    "schemaVersion": "4",
    "types": ["script"]
  }
}
